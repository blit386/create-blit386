name: Publish

on:
  push:
    tags:
      - 'v*'

jobs:
  publish:
    name: Publish to npm
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v4
        with:
          version: 10.26.2

      - name: Setup Node.js
        uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6
        with:
          node-version: '22.18.0'
          cache: 'pnpm'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify package versions match tag
        run: |
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          KIT_VERSION=$(node -p "require('./packages/kit/package.json').version")
          CBT_VERSION=$(node -p "require('./packages/create-blit-tech/package.json').version")

          if [ "$KIT_VERSION" != "$TAG_VERSION" ] || [ "$CBT_VERSION" != "$TAG_VERSION" ]; then
            echo "Tag v${TAG_VERSION} does not match package.json versions."
            echo "  @blit-tech/kit: ${KIT_VERSION}"
            echo "  create-blit-tech: ${CBT_VERSION}"
            exit 1
          fi

      - name: Build packages
        run: pnpm run build

      - name: Publish @blit-tech/kit
        run: |
          VERSION=$(node -p "require('./packages/kit/package.json').version")
          if npm view "@blit-tech/kit@${VERSION}" version >/dev/null 2>&1; then
            echo "@blit-tech/kit@${VERSION} already published; skipping."
          else
            pnpm publish --filter @blit-tech/kit --access public --no-git-checks
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish create-blit-tech
        run: |
          VERSION=$(node -p "require('./packages/create-blit-tech/package.json').version")
          if npm view "create-blit-tech@${VERSION}" version >/dev/null 2>&1; then
            echo "create-blit-tech@${VERSION} already published; skipping."
          else
            pnpm publish --filter create-blit-tech --access public --no-git-checks
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
